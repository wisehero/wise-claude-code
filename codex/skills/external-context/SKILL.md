---
name: external-context
description: 쿼리를 2-5개 facet으로 분해하여 context7 공식 문서 조회와 웹 검색/열람을 병렬로 수행하고 외부 문서·레퍼런스를 통합 수집하는 Codex용 리서치 스킬. "공식 문서 찾아줘", "~에 대해 조사해줘", "베스트 프랙티스 찾아줘", "버전별 차이 알려줘", "~랑 ~ 비교해줘" 같은 요청에 사용. 팩트체크, 라이브러리 선택, 패턴 리서치, 버전 호환성 확인 등에 활용한다. 단발 검색이 아니라 **다면적 리서치**가 필요한 경우 사용한다.
metadata:
  version: 1.3.0
---
<!-- level: 4 (내부 복잡도 티어 메타, 런타임 키 아님) -->


# External Context

외부 문서·레퍼런스·컨텍스트를 수집하는 Codex용 스킬. 쿼리를 2-5개의 독립적인 facet으로 분해하고, 각 facet을 context7 공식 문서 조회와 웹 검색/열람으로 조사한 뒤, 결과를 통합된 형식으로 제시한다.

> **참고**: 본 스킬은 [oh-my-claudecode](https://github.com/Yeachan-Heo/oh-my-claudecode/blob/main/skills/external-context/SKILL.md)의 `external-context`(MIT License)를 한국어 레포 컨벤션에 맞춰 재작성한 것이다. Copyright (c) Yeachan-Heo.

## 사용법

```
/external-context <주제 또는 질문>
```

### 예시

```
/external-context Node.js JWT 토큰 rotation 베스트 프랙티스
/external-context Prisma vs Drizzle ORM PostgreSQL 비교
/external-context React Server Components 최신 패턴과 관례
/external-context Spring Boot 3.x @Transactional의 REQUIRES_NEW 동작
```

## 프로토콜

### 1단계: Facet 분해

쿼리를 받으면 2-5개의 독립적인 검색 facet으로 분해한다. 각 facet은 병렬 검색 가능한 독립 관점이어야 한다 — 서로 내용이 겹치면 병렬화의 이득이 사라진다. 비교형 쿼리에서는 이 "겹침"을 **축 간 독립성**으로 재해석한다 — 각 facet은 비교 축이 되고, 각 축 안에서 A와 B를 동시에 다룬다.

#### go/no-go 체크리스트

분해를 시작하기 전에 쿼리가 아래 4가지 조건 중 **2개 이상**을 만족하는지 확인한다. 2개 미만이면 이 스킬을 쓰지 말고 Troubleshooting의 "facet이 1개로 환원되는 경우" 대안 경로로 리다이렉트한다:

- **(a)** 여러 **버전/시점** 간 비교가 필요한가? _(예: React 18 vs 19)_
- **(b)** 여러 **라이브러리/API** 간 비교가 필요한가? _(예: Prisma vs Drizzle)_
- **(c)** 여러 **관점**(정의·구현·예시·함정·마이그레이션 등)을 한 번에 다뤄야 하는가?
- **(d)** 서로 다른 **1차 소스**(공식 문서·RFC·이슈 트래커·벤치마크 등)를 교차 확인해야 하는가?

**경계 케이스**: facet 2개가 나왔지만 둘 다 같은 **단일 문서 페이지**로 수렴하면 단일 facet으로 간주한다 — 서로 다른 소스가 아니라 같은 소스의 다른 단락일 뿐이어서 병렬화 이득이 없다.

#### 정형 쿼리 유형별 권장 분해 축

description에 나열된 트리거 유형들은 분해 축이 유형별로 갈린다. 기본 원칙은 동일하다 — facet 축끼리 직교해야 하고 각 축 안에서 필요한 모든 대상(제품·버전·관점)을 한 번에 다룬다:

| 쿼리 유형       | 권장 분해 축                 | 예시                     |
| --------------- | ---------------------------- | ------------------------ |
| 비교            | 관심 축(아키·성능·DX·생태계) | `Prisma vs Drizzle`      |
| 버전 호환성     | 버전축 + 마이그레이션        | `React 18 vs 19 RSC`     |
| 라이브러리 선택 | 용도·생태계·유지보수·제약    | `로깅 라이브러리 추천`   |
| 패턴 리서치     | 정의·사례·함정·변형          | `RSC 데이터 페칭 패턴`   |

출력 형식:

```markdown
## 검색 분해

**쿼리:** <원본 쿼리>

### Facet 1: <facet 이름>
- **검색 초점:** 무엇을 찾을 것인가
- **소스:** 공식 문서, GitHub, 블로그 등

### Facet 2: <facet 이름>
...
```

### 2단계: 병렬 리서치 실행

각 facet을 현재 Codex 세션에서 사용 가능한 리서치 도구로 병렬 조사한다. 라이브러리·프레임워크·SDK·CLI·클라우드 서비스 문서 질의는 먼저 `tool_search`로 `context7` 문서 도구를 찾아 사용한다. 일반 웹, 논문, 표준, 뉴스성 자료는 웹 검색/열람 도구를 사용한다.

```
Facet 1: context7 또는 웹 검색으로 공식 문서/1차 출처 확인
Facet 2: context7 또는 웹 검색으로 공식 문서/1차 출처 확인
```

**최대 병렬 facet 수: 5**

### 3단계: 통합 출력 형식

리서치 결과를 아래 형식으로 통합하여 사용자에게 제시한다. 섹션 순서와 헤더 레벨을 그대로 따른다:

```markdown
## External Context: <쿼리>

### 핵심 발견

1. **<발견 사항>** — 출처: [제목](URL)
2. **<발견 사항>** — 출처: [제목](URL)

(3~5개 권장. facet 수와 쿼리 크기에 맞춰 조정하되, 7개 이상이면 덜 중요한 것을 `상세 결과`로 옮긴다)

### 상세 결과

#### Facet 1: <이름>
<인용 포함 통합 발견>

#### Facet 2: <이름>
<인용 포함 통합 발견>

### 충돌 · 전환 · 차이 지점

facet 간 결과가 서로 모순되거나, 버전축에서 전환되거나, 비교 대상 간에 본질적으로 갈리는 지점을 여기에 명시한다. 아래 세 가지 타입 중 해당하는 것만 넣고, 모두 없으면 이 섹션은 생략한다:

- **진짜 충돌** — 서로 다른 출처가 모순된 주장을 할 때. "출처 A는 X라고 하고, 출처 B는 Y라고 함 — 버전/맥락 차이로 **추정**" 형식. 한쪽을 임의로 택하지 않는다.
- **시간축 전환** — 버전/시점에 따른 API·동작 변경. "v1에서 X → v2에서 Y" 형식으로 **확정 표기**한다. 추정이 아니라 사실이므로 "추정" 표현을 쓰지 않는다.
- **비교 차이** — A vs B 비교형 쿼리의 본질적 차이. 이 타입은 `### 상세 결과` 안 각 facet에 녹여 쓰고, 이 섹션에는 중복으로 옮기지 않는다.

### 출처

- [출처 1](URL) — Facet 1, 3에서 확인됨
- [출처 2](URL) — Facet 2에서 확인됨
```

통합 시 중복된 출처는 하나로 합치되, 어느 facet에서 발견되었는지 교차 참조가 필요한 경우에는 `### 출처` 항목 뒤에 "Facet 1, 3에서 확인됨" 식으로 표기한다.

## 설정

- **최대 병렬 facet 수**: 5
- **트리거**: 매직 키워드 없음 — 명시적 `/external-context` 호출만
- **facet 최소 수**: 2 (단일 검색으로 충분하면 이 스킬을 쓰지 않고 바로 웹 검색/열람 호출)
- **facet 최대 수**: 5 (과잉 분해는 통합 비용을 키운다)

## 다른 스킬과의 연동

### `/study-helper`에서의 호출

`study-helper`의 3단계(팩트체크)에서 이 스킬을 호출한다. 학습 노트의 기술적 정확성 검증이 핵심 용도이므로, 팩트체크 모드에서는 다음 원칙을 따른다:

- **버전 기준**: 현재 널리 사용되는 최신 안정 버전(Latest Stable)을 기본으로 검증한다.
- **레거시 보완**: 노트에 이미 언급된 과거 버전만 한 줄로 간략히 언급한다. 노트에 없는 레거시 내용을 새로 추가하지 않는다.
- **수정 제안 근거**: 팩트체크로 수정을 제안할 때는 반드시 공식 문서 URL을 함께 제시한다.

### 독립 사용

`study-helper`와 무관하게 라이브러리 선택, API 사용법 조회, 패턴 비교, 버전 차이 확인 같은 일반 리서치 용도로도 사용 가능하다.

## Troubleshooting

**facet이 1개로 환원되는 경우**
- 원인: 쿼리가 이미 충분히 좁아서 분해할 관점이 없음 (1단계 go/no-go 체크리스트에서 2개 미만 통과)
- 해결: 이 스킬을 쓰지 말고 아래 대안 중 **쿼리 성격에 맞는 경로**를 선택한다:
  - **라이브러리·프레임워크 공식 문서 질의** → context7 도구를 직접 호출한다 (가장 빠르고 정확). 먼저 `tool_search`로 context7 도구를 노출한 뒤 라이브러리 ID를 찾고 문서 질의를 넘긴다.
  - **일반 웹 검색 / 블로그 / 비공식 레퍼런스** → 웹 검색/열람 도구를 직접 호출한다.
  - **여러 소스 교차 확인은 필요하지만 facet 분해는 과한 경우** → context7 + 웹 검색을 1회씩만 수행하고 종합한다.

**context7가 응답하지 않는 경우**
- 원인: MCP 서버 연결 실패 또는 해당 라이브러리 문서 미지원
- 해결: 공식 웹 문서를 웹 검색/열람으로 직접 확인한다. 단, 결과 품질은 context7 사용 시보다 낮을 수 있으므로 출처 신뢰도를 더 명확히 표시한다.

**출처 결과가 서로 다르게 나온 경우**
- 원인: 출처마다 버전·시점·관점이 다름 — 아래 세 타입 중 하나에 해당한다
- 해결: 통합 단계의 `### 충돌 · 전환 · 차이 지점` 섹션에 **타입별로 구분**해서 기록한다:
  - **진짜 충돌** (서로 다른 출처의 모순된 주장) → "버전/맥락 차이로 추정" 표현, 한쪽을 임의로 택하지 않는다
  - **시간축 전환** (버전에 따른 API·동작 변경) → "v1에서 X → v2에서 Y"로 확정 표기 (추정 아님)
  - **비교 차이** (A vs B 비교 쿼리의 본질적 차이) → 이 섹션에 중복으로 옮기지 말고 `### 상세 결과`의 해당 facet에만 기술한다
